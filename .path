# Add `gnubin` to the `$PATH`. It contains the coreutils installed via brew.
PATH="$(brew --prefix coreutils)/libexec/gnubin:$PATH"
# add gnu-sed to PATH (replaces BSD sed)
export PATH="/usr/local/opt/gnu-sed/libexec/gnubin:$PATH"
